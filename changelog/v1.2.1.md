# v1.2.1

## Config Compatibility
- Restore missing legacy config fields in `python/config/config.example.json`, including `vad_max_merge_size`
- Preserve `WEBUI_SETTING` defaults in the JSON-based config flow and show them again in the Settings UI
- Import missing or still-default values from a legacy `config.ini` into `config.json` automatically when present

## Transcription Fixes
- Pass `vad_max_merge_size` through the headless CLI bridge so the runtime behavior matches the migrated config
- Improve compatibility for users moving from the older INI-based project layout to the current JSON-based setup

## Docs
- Update README to document the restored `WEBUI_SETTING` block and legacy config migration behavior
