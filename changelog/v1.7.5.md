# v1.7.5

Patch release that fixes missing horizontal padding on **every dialog button** that didn't declare an explicit `width`.

## Fixed

- **Dialog buttons looked padding-less** — the base `.btn-primary` and `.btn-secondary` rules only declared `height`, not horizontal padding; container-level `width` declarations (e.g. `.modal-actions .btn-primary { width: 160px }`) were what gave them breathing room elsewhere in the app. Any dialog that didn't set a per-button width — the **Error dialog** (`複製錯誤資訊` / `關閉`), the **Install ffmpeg dialog** (`關閉`), and any future dialog in the same family — ended up with buttons that collapsed to their text content width, border hugging the label, which read as "no padding at all". Moved the padding to the base button rule (`padding: 0 18px; min-width: 96px` for `.btn-primary`, `padding: 0 18px; min-width: 88px` for `.btn-secondary`) so every auto-sized button gets sensible breathing room by default, while fixed-width instances keep working unchanged thanks to the global `box-sizing: border-box`.

## Migration notes

No user action required. Pure CSS fix.
