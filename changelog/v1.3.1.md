# v1.3.1

## Windows Quit Fix
- Fix a main-process window close loop that could prevent the app from closing on Windows
- Add explicit app quit / force-close guards so a forced close does not re-trigger the close interceptor
- Keep the transcription-running warning dialog, but allow the app to exit cleanly after the user confirms force close

## Notes
- This is an emergency test release focused on Windows app shutdown behavior
