# v1.8.4

Patch release with platform wording fix.

## Fixed

- **"Show in Finder" replaced with platform-neutral "Show in folder"** — the button label and help-article references used macOS-specific "Finder" wording, which was confusing on Windows where the file manager is called File Explorer. Changed to "Show in folder" (en) / "在資料夾中顯示" (zh-TW) across `common.json`, `queue.json`, and `help.json`.

## Migration notes

No user action required.
